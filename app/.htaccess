RewriteEngine On

# Ne pas réécrire les fichiers existants (CSS, JS, images, etc.)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Réécrire tout le reste vers index.php
RewriteRule ^ index.php [QSA,L]
